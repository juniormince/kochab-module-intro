module.exports = (array) => {
    let sum = 0;
    array.forEach((number) => {
        sum += number;
    });
    return sum;

    // for(number of array){
    //     sum += number;
    // }
    //return sum;
    
};